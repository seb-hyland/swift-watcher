build_stages = [
  { name = "Prepare build", program = "cp", args = [
    "-r",
    "../../../../igem/wiki/.",
    "./"
  ] },
  { name = "Export from Notion", program = "npm", args = ["run", "export"] },
  { name = "Build wiki", program = "sh", args = [
    "-c",
    "FORCE_COLOR=1 npm run build -w web -- --silent"
  ] },
  { name = "Cleanup", program = "rm", args = [
    "-rf",
    "node_modules",
    "web/node_modules",
    "package-lock.json"
  ] }
]
artifact_path = "web/dist"
